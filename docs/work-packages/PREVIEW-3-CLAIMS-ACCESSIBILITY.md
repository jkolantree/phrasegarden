# Preview 3 claims and accessibility evidence

Status: completed local checkpoint after failure-directed mobile repair

## OBJECTIVE

Make the proposed Preview 3 public description match the current product and
capture one current 320 px Written Builder screenshot with Advanced settings
closed, so the later source qualification begins from truthful claims and a
complete named screenshot matrix.

## SOURCE OF TRUTH

- `docs/PRODUCT.md`
- `docs/ACCESSIBILITY.md`
- `docs/EVALUATION.md`
- `docs/RELEASE-WORKFLOW.md`
- `docs/work-packages/PREVIEW-3-PUBLICATION.md`
- `docs/work-packages/PREVIEW-3-BEGINNER-JOURNEY.md`
- `tests/e2e/preview.spec.ts`
- Checkpoints `db85ed4a09f2e960ce0f6a31f84844b6e719bdf6`,
  `3c2a6061c68817fdd1d1718bd00b97ab9dd46f6e`, and
  `4d9002f0220726f195bf1bd473d69d24dfdaff31`
- Release-audit asset-order checkpoint
  `d6cb44888dd3852076e97726fddc795a3c52afe9`
- Mobile-select clarity checkpoint
  `7fb32c7f76c83b3a118e44c1859f3896fe703c94`

## IN SCOPE

Exact owned paths:

- `README.md`
- `docs/ACCESSIBILITY.md`
- `docs/LIMITATIONS.md`
- `docs/PROJECT-STATE.md`
- `docs/RELEASE-NOTES.md`
- `docs/TRACEABILITY.md`
- `docs/work-packages/PREVIEW-3-CLAIMS-ACCESSIBILITY.md`

Named changes:

- Describe French, German, Italian, Spanish, and region-unspecified Portuguese
  as identity-only Generic profiles.
- Describe the beginner-facing direct-create and language-presentation changes.
- Replace the stale eleven-journey claim with the exact current count.
- Describe the current `builder-written-mobile-320-closed.png` generated by the
  committed sequential browser source while Advanced settings are closed.
- Preserve honest limits on linguistic review, accessibility, IME, zoom,
  forced colors, screen readers, browser coverage, and publication state.

## OUT OF SCOPE

- Compiler, recipe, profile, registry, pack, or UI behavior changes
- New languages, regional Portuguese profiles, or support-tier promotion
- Source-manifest or package-generation tooling
- Source freeze, release build, archive generation, packaging, push, tag,
  release, deployment, or production verification
- Manual assistive-technology, real-device IME, or model evaluation

## ACCEPTANCE

- `CA-01`: Public Preview 3 notes no longer claim profiles or registry
  provenance are unchanged.
- `CA-02`: README, release notes, and limitations name the five new identities
  without implying pair-specific content, dialect neutrality, or review.
- `CA-03`: Beginner-facing presentation is described without claiming the app
  translates, teaches, or runs a model.
- `CA-04`: Accessibility documentation states twelve sequential Edge journeys.
- `CA-05`: The committed browser source captures a current full-page Written
  Builder from a 320×900 viewport with Advanced settings closed at the existing
  filename; the artifact is 320×3820 pixels.
- `CA-06`: The current capture is nonblank; both selected values are complete;
  there is no page-boundary clipping, overlap, or horizontal overflow; the
  primary action is clear; and Woven Conversation remains consistent.
- `CA-07`: Generated prompt bytes, compiler snapshots, versions, support
  resolution, and runtime privacy behavior remain unchanged.
- `CA-08`: Exact seven-path scope, focused/full checks, both typechecks, one
  development build, release audit, sequential Edge/axe lane, archive tests,
  forbidden-domain scan, diff check, and independent review pass.

## VERIFICATION

Use the bundled Node and Python runtimes already recorded by the project.

1. Check exact owned paths, index state, `git diff --check`, and net lines.
2. Use the exact focused 4/4 and full 311/311 results bound to the committed
   mobile-select source.
3. Run both TypeScript configurations.
4. Run the 20 archive-verifier regressions.
5. Require zero forbidden-domain matches in `src/domain`.
6. Run one development `pnpm build`, then `pnpm audit:release`.
7. Run `PLAYWRIGHT_CHANNEL=msedge pnpm test:e2e:dist` sequentially.
8. Re-run the release audit and require unchanged built bytes.
9. Inspect the current full-page closed-Advanced capture from the 320×900
   viewport directly and inventory the full current screenshot set. Do not
   count the preexisting orphan or either returned capture.
10. Obtain an independent read-only P1/P2/P3 review on exact bytes.

These are development/regression checks, not the later frozen-source run.

## STOP CONDITIONS

- A public statement would require an unsupported linguistic, accessibility,
  runtime, or publication claim.
- The browser source cannot capture the required current state without changing
  product behavior.
- Any generated prompt, snapshot, support result, or protected release byte
  changes unexpectedly.
- A baseline check fails for a reason outside this exact package.
- The package exceeds 700 net lines or requires source-freeze/package tooling.

## HANDOFF

Report exact files and checkpoint identity; `CA-01`–`CA-08`; test counts;
capture path, size, SHA-256, viewport, state, and visual verdict; known manual
evidence gaps; independent review; and the next eligible deterministic
source-manifest/package-generation package. Then stop before source freeze.
