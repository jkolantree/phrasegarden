# PhraseGarden `0.1.0-preview.5` local packaging evidence

Status: exact local seven-path content record; review, containing-commit,
workflow, deployment, and public outcomes require their own evidence

Recorded locally: 2026-08-19

## Identity and scope

- Source (`S5`): `16ad1fbf964e4ee6084457d27208c17ae5d413e9`
- Source tree: `0f6e8e0c3deea74766725122379d969c850a4c1f`
- Sole source parent: `aa69bea6146918df7e359ec4488eadceede1017c`
- Source manifest: 31,497 bytes, 162 files, 1,894,747 payload bytes,
  SHA-256
  `C22DC0C2035768978E130AD5E471E2F432F9BDDAC067E08CB45D95194B28772E`
- Intended Pages target: <https://jkolantree.github.io/phrasegarden/>
- No Preview 5 tag or GitHub release is authorized by this package.

This package makes the human action visually direct: choose, make
instructions, copy, and paste them into another tool. PhraseGarden still makes
instructions rather than translating or running a model. English↔Japanese is
Preview with external linguistic review incomplete; every other direction is
Generic. Portuguese `pt` is region-unspecified. Voice and Interpreter retain
their stated destination-tool and one-way limits.

No runtime AI, backend, account, storage, telemetry, model chooser, or new
language was added. This record does not claim linguistic truth, reviewer
qualification, WCAG conformance, an independent assistive-technology matrix,
release readiness, deployment, or current public bytes.

## Qualified distributable bytes

| Pages path | Bytes | SHA-256 |
|---|---:|---|
| `assets/index-mdmeC2AO.css` | 23,261 | `20F89F6D80C48DA4CA0B8E525D88F17367F621FFD735AAFE4901DA25EFC9F8C9` |
| `assets/index-xii-UK5o.js` | 161,647 | `542AC0D876AE56879DD53B28705959DFEF68FF08B1A0C17958D09CDD7EF10190` |
| `index.html` | 899 | `8EB3C308C18C5694B145A8AC341B7078E999A8A746B82FA526B456231EBF69FB` |

The successful pnpm 11.9.0 build processed 53 modules. Release audit before and
after browser qualification produced the same paths, lengths, and hashes. The
release manifest and ZIP contain exactly these three files and no source map.

## Local package bytes

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| `release/phrasegarden-0.1.0-preview.5-pages.zip` | 186,175 | `33BB1A8F8FA23B6B5D1DE0D727CA29CC19CBB33861ECD8B611C2C4C76F883458` |
| `release/phrasegarden-0.1.0-preview.5-pages-manifest.json` | 976 | `2E48975AC719D689F312A17D8997E4983E13F36245B62BBF1D826F7995E7DEA0` |
| `SHA256SUMS` | 1,716 | `0F551D3C522C89244A964E1C0F427C9C3A14B13E2DF3219692529C89DDA19228` |

The ZIP has three ordered, stored members with canonical metadata and bytes
equal to `dist`. The ledger preserves the exact 1,480-byte Preview 4 ledger,
SHA-256
`CB04A67C584205E12527C3FE3C5666B809BFD18CAE5F1E3ADCA51C5255E7FB7F`,
then appends only the Preview 5 ZIP row followed by the manifest row.

The seven-row source/stage/dist fingerprint is
`B25BAFEA7EDF220D45DD8ACCF216A30C3ACCDC04777A391431808F2CE530EF8B`.
Independent security and product/language/accessibility reviewers returned
zero-finding PASS on those exact bytes. One promotion process copied the stage
to the three local final paths without regeneration; direct comparison proved
stage equality. The promoted-three fingerprint is
`D2D2CCA19DF1C89DBC29773644E065433C5B9D7C14C22CB2B52FE8E073AAE945`.

## Local qualification

- CPython 3.12.13, Node.js 24, pnpm 11.9.0, TypeScript 7.0.2, Vite 8.1.5,
  Vitest 4.1.10, Playwright 1.61.1, and axe 4.12.1 were the named runtimes.
- The complete deterministic application suite passed 324/324 on the final
  source bytes; canonical prompt snapshots remained unchanged.
- The 58/58 Python release/security run binds the unchanged release-tool and
  release-test bytes. Those files did not change during the final three-path
  UX repair and were rebound by the final release review; the Python suite was
  not rerun after `S5`.
- Both TypeScript configurations passed in the successful release build.
- Twelve sequential Microsoft Edge/axe journeys passed on the packaged bytes,
  including direct creation, settings, Voice, Interpreter, Generic languages,
  copy/download, offline-after-load, keyboard/focus, IME, bidi, reduced motion,
  and 200%/400%-equivalent reflow.
- Fresh mobile and desktop Home, Builder, and Review captures received direct
  visual inspection. Copy fits in the first 320 × 900 Review viewport after
  the support and name-reading limitation; the complete text remains visible
  in a bounded focusable region.
- Source verification passed before build qualification and after browser
  qualification. Both independent staged-package reviews rebound exact `S5`,
  its manifest, `dist`, ZIP, release manifest, and ledger.

## Invalid harness observations

The first release-build launch found pnpm 11.9.0 but omitted the pinned Node
directory from the child `PATH`; `tsc` did not start and `dist` remained absent.
Exact `S5` and its source manifest were reverified before the corrected
environment produced the only successful build. The failed launch supplies no
build or product evidence.

Restricted-sandbox source and package commands were also rejected because Git
correctly treated the owner-bound repository as unsafe for the sandbox account.
The same frozen operations ran in the repository owner's context. Those
rejections supply no package evidence and did not change the candidate.

An early poll inspected final paths while the one promotion process was still
running. The absence observation is invalid timing evidence. No promotion
retry occurred; all promoted-byte claims use the later stable state.

## Packaging and public boundary

The containing commit qualifies as `P5` only if it has sole parent `S5`, changes
only the seven paths in `PREVIEW-5-PUBLICATION.md`, receives an independent
exact-byte review, passes the pinned committed-package verifier, and extracts
bytes identical to the reviewed stage and `dist`.

After local verification, the already authorized Pages update may advance
exact `P5` to `jkolantree/phrasegarden` `main` only if fresh remote preflight
shows the expected predecessor. The triggered workflow, deployment result, and
public HTML/assets require separate observation and byte comparison. No tag or
GitHub release is part of that authorization.
